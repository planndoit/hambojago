# 함보자고 프로젝트 진행 순서

## 1. MVP 범위 확정

`README.md` 기준으로 이 프로젝트의 MVP는 날짜 조율에만 집중한다.

- 생성자는 로그인 후 약속을 만들고 링크를 공유한다.
- 참여자는 로그인 없이 공유 링크로 들어와 가능한 날짜만 선택한다.
- 참여 완료 후 결과 화면에서 유력 날짜와 날짜별 참여자를 확인한다.
- 참여자는 이름과 4자리 PIN, 또는 저장된 `edit_token`으로 본인 선택을 수정할 수 있다.
- 장소 투표, 정산, 타임라인, 미니게임은 MVP 범위에서 제외한다.

## 2. 기술 기반 구축

추천 기술 스택에 맞춰 기본 실행 환경을 먼저 구성한다.

- Next.js 프로젝트 생성
- Supabase 프로젝트 생성
- PostgreSQL 데이터베이스 연결
- Vercel 배포 프로젝트 연결
- 환경 변수 구성
- 로컬 개발 실행 확인

## 3. 데이터 모델 설계

사용자 흐름과 수정 방식을 지원하는 최소 데이터 모델을 먼저 만든다.

- 생성자 계정 데이터
- 약속 데이터
- 후보 날짜 데이터
- 참여자 데이터
- 참여자별 가능 날짜 데이터

최소로 필요한 핵심 테이블은 다음과 같다.

- `events`: 약속 이름, 설명, 생성자, 날짜 범위, 공유 식별자
- `event_dates`: 약속별 후보 날짜
- `participants`: 참여자 이름, PIN 해시, 수정 토큰 해시
- `availability`: 참여자별 선택 날짜

## 4. 생성자 인증 구현

약속 생성자는 로그인이 필요하므로 생성 플로우보다 인증을 먼저 구현한다.

- 회원가입
- 로그인
- 로그아웃
- 로그인 상태 확인
- 비로그인 사용자의 약속 생성 접근 제한

## 5. 약속 생성 플로우 구현

생성자가 링크를 만들 수 있는 최소 기능을 구현한다.

- 첫 진입 화면
- 약속 만들기 버튼
- 로그인 또는 회원가입 유도
- 약속 이름 입력
- 약속 설명 선택 입력
- 날짜 범위 설정
- 약속 저장
- 공유 링크 생성

## 6. 참여자 날짜 선택 화면 구현

MVP의 핵심 목표가 10초 안에 날짜 선택을 끝내는 것이므로 참여자 화면을 우선순위 높게 구현한다.

- 공유 링크 진입
- 약속 이름과 한 줄 설명 표시
- 달력 UI 표시
- 후보 날짜 리스트 표시
- 달력과 리스트 선택 상태 동기화
- 날짜 다중 선택
- 선택 개수 표시
- 선택이 없을 때 선택 완료 버튼 비활성화

## 7. 참여자 제출 구현

로그인 없는 참여와 이후 수정을 동시에 고려해 제출 로직을 구현한다.

- 선택 완료 버튼
- 이름 입력
- 4자리 PIN 입력
- PIN 해시 저장
- 참여자 생성
- 선택 날짜 저장
- `edit_token` 발급
- `edit_token` 해시 저장
- 클라이언트 `localStorage`에 원문 `edit_token` 저장

## 8. 결과 화면 구현

참여 직후 명확한 피드백을 제공하는 결과 화면을 구현한다.

- 현재 참여 인원 표시
- 가장 많이 선택된 유력 날짜 강조
- 날짜별 선택 인원 수 표시
- 날짜별 참여자 목록 표시
- 내 선택 날짜 시각적 구분

## 9. 참여자 수정 기능 구현

이미 참여한 사용자가 본인 데이터만 수정할 수 있도록 구현한다.

- 공유 링크 재접속 시 `localStorage`의 `edit_token` 확인
- 서버에서 `edit_token` 검증
- 검증 성공 시 본인 선택 데이터 로드
- 날짜 선택 수정
- 이름과 PIN 기반 보조 수정
- 타인 데이터 수정 차단

## 10. 공유와 OG 정보 구성

카카오톡 등 메신저 공유 시 서비스 목적이 바로 보이도록 링크 정보를 구성한다.

- 공유 URL 생성
- OG Title 설정
- OG Description 설정
- 공유 링크 접근 동작 확인

## 11. 모바일 UX 점검

서비스 핵심 사용 환경을 모바일로 보고 UI를 점검한다.

- 달력 터치 영역 확인
- 날짜 선택과 해제 동작 확인
- 하단 CTA 접근성 확인
- 첫 화면에서 불필요한 설명 제거
- 참여 완료까지 10초 목표 흐름 확인

## 12. 보안과 데이터 검증

참여자 수정과 인증 관련 데이터가 의도대로 보호되는지 확인한다.

- PIN 원문 저장 금지
- `edit_token` 서버 원문 저장 금지
- `localStorage`에는 토큰 원문만 저장
- 서버에는 토큰 해시만 저장
- 이벤트별 데이터 접근 제한
- 참여자 본인 데이터만 수정 가능

## 13. 배포 준비

MVP 흐름 전체가 동작하는 상태에서 배포 환경을 정리한다.

- Supabase 운영 환경 설정
- Vercel 환경 변수 설정
- 프로덕션 빌드 확인
- 배포 후 공유 링크 동작 확인
- 모바일 브라우저에서 생성자 플로우 확인
- 모바일 브라우저에서 참여자 플로우 확인

## 14. MVP 완료 기준

다음 항목이 모두 가능하면 MVP를 완료한 것으로 본다.

- 생성자가 로그인 후 약속을 만들 수 있다.
- 생성자가 공유 링크를 만들 수 있다.
- 참여자가 로그인 없이 공유 링크로 들어갈 수 있다.
- 참여자가 가능한 날짜를 10초 안에 선택하고 제출할 수 있다.
- 참여 완료 후 결과 화면에서 유력 날짜를 확인할 수 있다.
- 참여자가 본인 선택을 수정할 수 있다.
- 타인의 선택은 수정할 수 없다.
